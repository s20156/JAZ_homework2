spring:
  config:
    activate:
      on-profile: prod
my:
  custom:
    profileGreeting: Production profile
    initBean: false


---
spring:
  config:
    activate:
      on-profile: dev
my:
  custom:
    profileGreeting: Development profile
    initBean: true